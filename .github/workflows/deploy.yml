name: Deploy to GitHub Pages

on:
  push:
    branches: [ main, reasonable-comp-calculator ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: reasonablecomp/package.json
    
    - name: Install dependencies
      run: |
        cd reasonablecomp
        npm install
    
    - name: Build Next.js app
      run: |
        cd reasonablecomp
        npm run build
        ls -la
        ls -la out/
    
    - name: Prepare deployment files
      run: |
        mkdir -p deploy
        if [ -d "reasonablecomp/out" ]; then
          cp -r reasonablecomp/out/* deploy/
        else
          echo "Build output directory not found. Checking for other output directories..."
          ls -la reasonablecomp/
          exit 1
        fi
    
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.PAT_TOKEN }}
        publish_dir: deploy
        destination_dir: reasonablecomp
        force_orphan: true 